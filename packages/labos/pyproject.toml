[project]
name = "labos"
version = "0.1.0"
description = "LabOS — reference research domain for AgentOS (ML replication)"
requires-python = ">=3.11"
dependencies = [
    "agentos",
    "scikit-learn>=1.3",
    "matplotlib>=3.7",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["labos*"]
